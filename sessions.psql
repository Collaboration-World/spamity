-- sessions

create table sessions (
  id char(32) not null,
  sid varchar(64),               -- required by Spamity
  a_session text not null,
  primary key (id)
);

-- For CGI::Session version >= 4.x

-- create table sessions (
--   id char(32) not null,
--   sid varchar(64),               -- required by Spamity
--   a_session bytea not null,
--   primary key (id)
-- );